# Leave these blank for local development. Vite proxies /api and /ws to FastAPI on port 8000.
# VITE_API_BASE_URL=
# VITE_WS_URL=

# Real backend is the default. Set to true only when you intentionally want Demo Mode.
VITE_USE_MOCK_MODE=false
